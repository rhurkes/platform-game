// 16px font is 10x18, 32px is 20x36, etc.
const fontsize = 32
const pixelsPerTileX = 20;
const pixelsPerTileY = 36;
const defaultDrawColor = '#ccc';
const dimensions = { rows: 16, columns: 16 };

export default {
  fontsize, pixelsPerTileX, pixelsPerTileY, defaultDrawColor, dimensions,
};
